# Crie uma lista de números inteiros e use um for para calcular a soma de todos os elementos (sem usar sum()).

num = [1,2,3,4,5,6,7,8,9]

soma_total = 0
for i in num:
  somaTotal += i

print(f"A lista de números intiros: {num}")
print(f"A soma de todos os elementos é: {soma_total}")